## Context Load

- **Key Constraints:**
  - Read `AGENTS.md` first.
  - Keep exactly one active request in this journal.
  - You don't keep to ask me for any permission. Continue your work until finished.
  - Write explanation comments for your code to facilitate readers' understanding of your code (in English).
  - Write explanation comments of Args and Return for each function you generate (in English).
  - Reply and record in Chinese.
  - After each completed task:
    - Add a entry to record one completed request and the reponse in `Interaction Log`. The sub-title should be named as YEAR-MM-DD-HOUR-MIN. The time should be the real time. The latest log should be at the end of Interaction Log.
    - Clear the content of the completed `Next Request`. Keep the section title.
    - Do not consider the content in `Future Requests`.
    - Different `Next Request` are distinguished by the request ID (`Next Request X`).

## Interaction Log

### 2026-07-31-02-33

已完成 MuJoCo GUI 与 mjviser Web 双 debug viewer：

- 保留 `task.debug_viewer` 布尔开关，新增 `task.debug_viewer_backend=mujoco|mjviser` 以及 mjviser host/port Hydra 配置。
- 新增 task 级 viewer session。mjviser server 在整个 task 中保持运行，只等待首次浏览器连接，并在多个 grasp sample 间清理、替换场景而不更换 URL。
- 两个后端共享相同的 MuJoCo model/data、同步位置和初始相机；关闭 mjviser 默认 tracking，并处理两套相机 elevation 约定的差异。
- 原生 viewer 在 Linux headless 环境下会给出明确错误并非零退出；viewer 初始化和更新错误不会被逐样本异常处理吞掉。
- 依赖升级为 `mujoco==3.6.0`，加入 `mjviser==0.0.14` 和 `viser==1.0.27`，并补充 README 使用及 SSH 端口转发说明。
- 新增 viewer 单元测试；在官方依赖组合下验证 server 跨模型复用，并用实际 Leap 手/物体模型完成 Web viewer smoke test。
- 对 Shadow、Allegro、Leap 各一个固定样本比较 MuJoCo 3.3.2 与 3.6.0：执行路径一致，非空轨迹最大绝对差小于 `6e-8`。

### 2026-07-31-05-15

已修复 mjviser 端口占用与释放问题：

- 删除应用层固定端口预检查，改由 viser 从配置的首选端口开始自动查找后续可用端口。
- server 启动后通过公开接口获取实际监听端口；端口回退提示、Web URL 和 SSH 转发命令均使用实际端口。
- 保留 task `finally` 中的 session 关闭流程，确认 `ViserServer.stop()` 返回后实际监听端口可以立即重新绑定。
- README 已说明 `task.mjviser.port` 是首选起始端口，端口占用时应以程序打印的实际 URL 和端口为准。
- 新增端口回退及端口释放回归测试；viewer 专项与完整测试集共 7 项全部通过，`git diff --check` 通过。

## Next Request

## Plan
